import React from "react";
import ReactDOM from "react-dom/client";
import MechConnect from "./Apps.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MechConnect />
  </React.StrictMode>
);
